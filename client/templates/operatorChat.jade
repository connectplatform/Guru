.well

  ul#chatTabs.nav.nav-tabs

    for chat in chats
      li: a(href="##{chat.renderedId}", data-toggle="tab") #{chat.visitor.username}

  .tab-content.operatorChat

    for chat in chats
      .tab-pane.chatWindow(id="#{chat.renderedId}", chatId="#{chat.id}")
        .chat-box.well.chat-display-box
          for msg in chat.history
            p #{msg.username}: #{msg.message}

        form.message-form.form-inline
          input(type="text", value="").message

          button.btn.btn-primary(type="submit").submit-button Send