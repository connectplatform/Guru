require 'sugar'
Object.extend()

#async = require 'async'
#should = require 'should'

throw new Error 'broken'

#show = (next) ->
  #console.log 'hey'
  #throw new Error 'broken'
  #next()

#async.auto {
  #show: show

#}, (err, result) ->
  #result.val.should.eql 1
